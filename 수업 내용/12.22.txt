Q)
매개변수가 있는 함수

콜백 함수

on 함수 bind 함수

함수 안에서 순차적으로 실행 되게 하는 함수 방식.

반환 함수

로드함수를 통한 선택자 선택(on 선택자)





width(), height(), offset(), position(), attr(), html(), text(), append(), prepend(), css(),

위 메소드를 잘 알아두자.
